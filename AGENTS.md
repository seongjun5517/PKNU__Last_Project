# Project-specific output rule

- Before creating or writing generated artifacts under this project's `outputs` directory, always ask the user for permission.
- If the user declines, save the artifacts under `%USERPROFILE%\Desktop\outputs` instead.
- Do not create either output directory until the user has answered.
